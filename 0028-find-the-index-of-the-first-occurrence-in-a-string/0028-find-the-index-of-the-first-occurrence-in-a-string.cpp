class Solution {
public:
    int strStr(string haystack, string needle) {
        if(haystack.find(needle)==string::npos){return -1;}
        return haystack.find(needle);
        // int a=0;
        // string b="";
        // for(int i=0;i<haystack.length();i++){
        //     if(needle[a]==haystack[i]){
        //         b.push_back(needle[a]);
        //         a++;
            }
        
        
    
};